# --- IDE ---
.idea/
.vscode/
*.iml

# --- OS ---
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
Thumbs.db
desktop.ini

# --- Real deployment config (keep the *.example.yaml only) ---
docker-compose.yaml
docker-compose.yml
docker-compose.override.yaml
docker-compose.override.yml

# --- Env / secrets ---
.env
.env.*
!.env.example
*.secret
secrets/
cf_token
*_token

# --- ACME / TLS state and certs (never commit private keys) ---
letsencrypt/
lego/
*.pem
*.key
*.crt
*.csr
traefik-tls.yml

# --- Misc ---
*.log
*.bak
*.tmp
*.swp
